# Local development uses the Vite proxy when this is empty.
# Production (Vercel): set this to your Render API base, e.g.
# VITE_API_BASE_URL=https://fieldverify-api.onrender.com/api/v1
VITE_API_BASE_URL=
